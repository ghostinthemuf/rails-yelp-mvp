<h1>All the restaurants</h1>
<%= link_to "Add a new joint", new_restaurant_path, class: "btn btn-primary" %>
<% @restaurants.each do |restaurant| %>
  <h2><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
  <p><%= restaurant.address %></p>
<% end %>
